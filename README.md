# Novel Frog

Novel Frog 是一个基于 `WXT + React + TypeScript` 的浏览器插件项目，聚焦小说搜索、抓取、缓存、阅读和导出。

当前仓库正在做工程化收敛，重构目标是：

- 单一数据模型
- 单一消息协议
- 单一存储实现
- 清晰的入口边界与业务边界

后续建议继续围绕 `src/features` 和 `src/shared` 演进，逐步替换历史遗留模块。
